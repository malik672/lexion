diff --git a/fynd-core/src/algorithm/mod.rs b/fynd-core/src/algorithm/mod.rs
--- a/fynd-core/src/algorithm/mod.rs
+++ b/fynd-core/src/algorithm/mod.rs
@@ -24,6 +24,7 @@
 pub mod bellman_ford;
+mod concavity_audit;
 mod exact_v2_refiner;
 mod exact_v2_search;
 pub mod most_liquid;
 pub mod path_frank_wolfe;
 pub(crate) mod paths;
diff --git a/fynd-core/src/algorithm/exact_v2_search.rs b/fynd-core/src/algorithm/exact_v2_search.rs
--- a/fynd-core/src/algorithm/exact_v2_search.rs
+++ b/fynd-core/src/algorithm/exact_v2_search.rs
@@ -38,6 +38,10 @@ fn symbolic_cut_census_enabled() -> bool {
     std::env::var_os("FYND_SYMBOLIC_ALLOCATION_CENSUS").is_some()
 }
 
+fn concavity_audit_enabled() -> bool {
+    std::env::var_os("FYND_CONCAVITY_AUDIT").is_some()
+}
+
 struct PathSearch<'a> {
     ctx: &'a BellmanFordContext,
@@ -618,7 +622,8 @@ fn discover_paths(
     total: &BigUint,
     max_hops: usize,
 ) -> Result<Vec<PathAllocation>, AlgorithmError> {
-    let collect_census = census_enabled() || symbolic_cut_census_enabled();
+    let collect_census =
+        census_enabled() || symbolic_cut_census_enabled() || concavity_audit_enabled();
     let mut search = PathSearch {
         ctx,
@@ -632,7 +637,10 @@ fn discover_paths(
     if census_enabled() {
         emit_language_census(&search.census_paths, total, ctx);
     }
     if symbolic_cut_census_enabled() {
         emit_symbolic_allocation_cut_census(&search.census_paths, total, ctx);
     }
+    if concavity_audit_enabled() {
+        super::concavity_audit::emit(&search.census_paths, total, ctx);
+    }
 
     search.paths.sort_unstable_by(|a, b| b.amount_out.cmp(&a.amount_out));
